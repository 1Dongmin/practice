#include <stdio.h>
int main(){
    int time, score = 0, sum = 0;
    char oxox[80];
    scanf("%d", &time);
    for(int i = 0; i < time; i++){
        scanf("%s", &oxox);
        for(int k = 0; k < 80; k++){
            if(oxox[k] == NULL) break;
            if(oxox[k] == O){
                
            }
        }
    }
}
