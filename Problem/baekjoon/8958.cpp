#include <stdio.h>
int main(){
    int time;
    char oxox[80];
    scanf("%d", &time);
    for(int i = 0; i < time; i++){
        scanf("%s", &oxox);
        for(int k = 0; k < 80; k++){
            if(oxox[k] == NULL) break;
            
        }
    }
    
}
